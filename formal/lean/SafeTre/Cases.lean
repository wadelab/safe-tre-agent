/-
GENERATED by scripts/gen_lean_catalogue.py — do not edit by hand.
tests/test_formal_lean_sync.py fails CI when this file drifts from the code.
-/

import SafeTre.Types

namespace SafeTre

/-- The covering enumeration: (spec, engine SQL, bound-parameter count).
The SQL strings are engine.compile_query's actual output; Proofs.lean
proves `render (compile spec)` reproduces every one exactly. -/
def cases : List (Spec × String × Nat) := [
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, [], []⟩,
   "SELECT CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" IN (?, ?) AND \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "lootbox_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"age_years\" IS NOT NULL AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, [], []⟩,
   "SELECT CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" IN (?, ?) AND \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"age_years\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, [], []⟩,
   "SELECT CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" IN (?, ?) AND \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "age_years", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"age_years\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"age_years\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, [], []⟩,
   "SELECT CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "purchase_events"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"lootbox_events\" IS NOT NULL AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, [], []⟩,
   "SELECT CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "lootbox_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"lootbox_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"lootbox_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, [], []⟩,
   "SELECT CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.corr, none, some "purchase_events", some "total_spend_gbp"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"purchase_events\", \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"purchase_events\" IS NOT NULL AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, [], []⟩,
   "SELECT COUNT(*) AS n FROM \"donor_spend\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"donor_spend\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", COUNT(*) AS n FROM \"donor_spend\" GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", COUNT(*) AS n FROM \"donor_spend\" GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"donor_spend\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, [], []⟩,
   "SELECT MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, [], []⟩,
   "SELECT MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, [], []⟩,
   "SELECT MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.mean, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, [], []⟩,
   "SELECT SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, [], []⟩,
   "SELECT SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, [], []⟩,
   "SELECT SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sum, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, [], []⟩,
   "SELECT SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "lootbox_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"lootbox_events\" * \"lootbox_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"lootbox_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, [], []⟩,
   "SELECT SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "purchase_events", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"purchase_events\" * \"purchase_events\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"purchase_events\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, [], []⟩,
   "SELECT SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" IN (?, ?) AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"_donor_spend_u\" WHERE \"age_years\" >= ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"donor_spend", ⟨.sumSq, some "total_spend_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"total_spend_gbp\" * \"total_spend_gbp\") AS value, COUNT(*) AS n FROM \"donor_spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"region\" != ? AND \"total_spend_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 4),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, [], []⟩,
   "SELECT CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.corr, none, some "amount_gbp", some "ingame_currency"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"amount_gbp\", \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"amount_gbp\" IS NOT NULL AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.count, none, none, none⟩, [], []⟩,
   "SELECT COUNT(*) AS n FROM \"spend\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", COUNT(*) AS n FROM \"spend\" GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", COUNT(*) AS n FROM \"spend\" GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, [], []⟩,
   "SELECT MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, [], []⟩,
   "SELECT MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.mean, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, [], []⟩,
   "SELECT SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, [], []⟩,
   "SELECT SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sum, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, [], []⟩,
   "SELECT SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "amount_gbp", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"amount_gbp\" * \"amount_gbp\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"amount_gbp\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, [], []⟩,
   "SELECT SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band", "age_rating"], []⟩,
   "SELECT \"age_band\", \"age_rating\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band", "age_rating", "contains_lootboxes"], []⟩,
   "SELECT \"age_band\", \"age_rating\", \"contains_lootboxes\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\", \"age_rating\", \"contains_lootboxes\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" IN (?, ?) AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_rating", .le, 1, some 3⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_rating\" <= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"_spend_u\" WHERE \"age_years\" >= ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"spend", ⟨.sumSq, some "ingame_currency", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"event_type", .eq, 1, none⟩, ⟨"age_rating", .gt, 1, some 3⟩, ⟨"genre", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"ingame_currency\" * \"ingame_currency\") AS value, COUNT(*) AS n FROM \"spend\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"event_type\" == ? AND \"age_rating\" > ? AND \"genre\" != ? AND \"ingame_currency\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, [], []⟩,
   "SELECT CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "monthly_spend_selfreport"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, [], []⟩,
   "SELECT CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, [], []⟩,
   "SELECT CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "igds_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"igds_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, [], []⟩,
   "SELECT CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "pgsi_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, [], []⟩,
   "SELECT CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "monthly_spend_selfreport", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"monthly_spend_selfreport\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"monthly_spend_selfreport\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, [], []⟩,
   "SELECT CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.corr, none, some "pgsi_score", some "wemwbs_score"⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", CORR(\"pgsi_score\", \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"pgsi_score\" IS NOT NULL AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, [], []⟩,
   "SELECT COUNT(*) AS n FROM \"wellbeing\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", COUNT(*) AS n FROM \"wellbeing\" GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", COUNT(*) AS n FROM \"wellbeing\" GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.count, none, none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, [], []⟩,
   "SELECT MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, [], []⟩,
   "SELECT MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, [], []⟩,
   "SELECT MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, [], []⟩,
   "SELECT MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.mean, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", MEAN(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, [], []⟩,
   "SELECT SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sum, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "igds_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"igds_score\" * \"igds_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"igds_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, [], []⟩,
   "SELECT SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "monthly_spend_selfreport", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"monthly_spend_selfreport\" * \"monthly_spend_selfreport\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"monthly_spend_selfreport\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "pgsi_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"pgsi_score\" * \"pgsi_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"pgsi_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, [], []⟩,
   "SELECT SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band", "device_os"], []⟩,
   "SELECT \"age_band\", \"device_os\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band", "device_os", "income_band"], []⟩,
   "SELECT \"age_band\", \"device_os\", \"income_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\", \"device_os\", \"income_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], []⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 0),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .isIn, 2, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" IN (?, ?) AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 2),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"wave", .le, 1, some 1⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"wave\" <= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_years", .ge, 1, some 18⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"_wellbeing_u\" WHERE \"age_years\" >= ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 1),
  (⟨"wellbeing", ⟨.sumSq, some "wemwbs_score", none, none⟩, ["age_band"], [⟨"age_band", .eq, 1, none⟩, ⟨"device_os", .eq, 1, none⟩, ⟨"income_band", .eq, 1, none⟩, ⟨"wave", .gt, 1, some 1⟩, ⟨"region", .ne, 1, none⟩]⟩,
   "SELECT \"age_band\", SUM(\"wemwbs_score\" * \"wemwbs_score\") AS value, COUNT(*) AS n FROM \"wellbeing\" WHERE \"age_band\" == ? AND \"device_os\" == ? AND \"income_band\" == ? AND \"wave\" > ? AND \"region\" != ? AND \"wemwbs_score\" IS NOT NULL GROUP BY \"age_band\" ORDER BY n DESC LIMIT 10000", 5)
]

end SafeTre
