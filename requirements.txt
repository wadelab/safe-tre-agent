pandas>=2.0
numpy>=1.24
pyyaml>=6.0
# optional — only needed for a real model backend (any OpenAI-compatible endpoint)
openai>=1.30
# optional — only for regenerating the write-up figure (scripts/make_figures.py)
matplotlib>=3.7
# optional — production disclosure control would wrap ACRO instead of the
# lightweight reimplementation in safetre/disclosure.py
# acro>=0.4
